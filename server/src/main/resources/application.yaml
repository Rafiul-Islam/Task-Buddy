spring:
  application:
    name: TaskBuddy
  datasource:
    url: jdbc:postgresql://localhost:5432/task_buddy
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

